# 调研简报：签名与日期合规性检查

**日期：** 2026-06-01  
**主题：** 自动检测移民表格（PDF）中签名和日期的质量问题

---

## 背景

移民案件表格（例如 I-140、ETA-9089、G-28）要求在特定字段中提供湿墨手写签名和清晰可读的日期。当文档以数字方式提交时，我们需要在律师助理审核之前，以程序化方式验证签名和日期是否满足合规要求。

你的任务是研究下列每种错误类型如何能够从 PDF 或栅格化页面图像中可靠地检测出来。

---

## 范围

针对以下 **6 种错误类型** 研究检测方法。对于每一种，你都应当提出至少一个具体、可实现的方案——最好有现成的库或模型作支撑。

---

## 错误类型

### 1. `SIGNATURE_MISSING`（缺少签名）
必填的签名字段中没有任何手写签名。

**研究问题：**
- 如何区分空白签名框与包含墨迹笔画的签名框？
- 哪些图像处理或基于模型的技术适合检测裁剪区域内是否存在墨迹？
- 如何避免因表格预印内容（线条、标签、水印）而产生误报？

---

### 2. `SIGNATURE_WRONG_BOX`（签名位置错误）
页面上存在签名，但似乎位于错误的位置——超出了指定的签名字段之外。

**研究问题：**
- 如何检测看起来像签名的墨迹笔画是否位于预期之外的区域？
- 如何从几何角度刻画"类似签名"的墨迹簇是什么样子？
- 如何衡量检测到的笔画簇与预期边界框之间的空间重叠程度？

---

### 3. `SIGNATURE_TOO_FAINT`（签名过浅）
签名存在，但过于浅淡或苍白，以致无法辨认或不具备法律效力。

**研究问题：**
- 哪些图像层面的指标（像素强度、对比度等）最能刻画墨迹的"浅淡"程度？
- 在测量深浅之前，如何将墨迹像素从背景中分离出来？
- 你会使用什么阈值或取值范围来将签名分类为浅淡 vs. 可接受？

---

### 4. `TYPED_SIGNATURE`（打字签名）
签名字段中包含的是打字文本，而非手写签名。

**研究问题：**
- 哪些视觉或结构特征可以区分打字文本和手写笔画？
- OCR 能否作为检测流程的一部分？如果可以，如何使用？
- 哪些信号表明框内内容是打字的（例如笔画宽度均匀、基线规整、字符间距一致）？

---

### 5. `PASTED_IMAGE_SIGNATURE`（粘贴图片签名）
签名是一张粘贴的图片（例如嵌入 PDF 的签名 PNG 或 JPEG），而非手写原件。

**研究问题：**
- 能否在不进行栅格化的情况下检测 PDF 原生结构中的嵌入图像对象？哪些库支持这一点？
- 如何判断某个嵌入图像是否位于签名字段内？
- 粘贴的图片签名与对湿墨页面的真实手写扫描件之间有什么区别？

---

### 6. `DATE_UNREADABLE`（日期无法识别）
日期字段存在，但内容缺失、难以辨认，或无法解析为有效日期。

**研究问题：**
- 如何使用 OCR 从表格中特定的日期框里提取文本？
- 移民表格中常见哪些日期格式（例如 mm/dd/yyyy、民国纪年）？
- 当 OCR 返回了文本但它不匹配任何已知日期格式时，如何处理？
- 如何处理完全空白的日期字段？

---

## 交付成果

对于每种错误类型，请记录：

1. **建议的检测方法** —— 用通俗语言描述方法（例如："裁剪签名框，应用自适应阈值化，统计连通的墨迹分量"）
2. **关键信号** —— 哪些指标、特征或模型输出驱动决策？
3. **库 / 工具** —— 你会使用哪些 Python 包或 API？（例如 OpenCV、PyMuPDF、pytesseract、视觉模型）
4. **失效模式** —— 哪些情况可能会骗过你的方法？你将如何应对？
5. **大致置信度估计** —— 这是一个高置信度的二元判断，还是需要带有审核区间的软评分？

---

## 约束与说明

- 输入是一个 PDF 文件（可能是扫描的栅格图，也可能是原生数字 PDF）。
- 你必须在裁剪层面工作：调用方会提供签名或日期字段在页面中的边界框。
- 在可能的情况下，方案应当是确定性且快速的；仅在更简单的方法不足以胜任时，才可接受基于 LLM 的方法。
- 每种错误类型都应当能够独立检测——避免相互耦合。
- 不要假设可以访问文档的任何先前版本。

---

## 入门资源

- PyMuPDF（`fitz`）—— PDF 解析与图像提取
- OpenCV（`cv2`）—— 图像处理
- 任意你选择的 OCR 库（例如 Google Document AI、Tesseract、Azure OCR）
- 关于手写体 vs. 印刷体检测、签名验证的学术文献

---

## 有疑问？

如果任何错误类型含义不清，欢迎随时联系。请优先考虑实用、可实现的方法，而非理论上的完备性。
